{
  "name": "past_feed",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "tsc -w --pretty",
    "start": "node main.js",
    "build": "rm -r dist && tsc",
    "docker-build": "docker build -f ./images/past-feed.Dockerfile -t past-feed:latest .",
    "docker-compose-up": "docker-compose up -d",
    "docker-compose-down": "docker-compose down -v",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "nano": "^8.1.0",
    "puppeteer": "^1.14.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.16.1",
    "@types/puppeteer": "^1.12.3"
  }
}
